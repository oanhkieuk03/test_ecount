# Manual Test Cases — Forgot Login

**Feature:** Forgot your login information? (Password Recovery Popup)  
**URL:** `[configured via FORGOT_URL env variable]`

---

| ID | Category | Priority | Title | Steps | Test Data | Expected Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **FLI_001** | Functional | P0 | Open popup via "Forgot your login information?" link | 1. Go to the login page<br>2. Click "Forgot your login information?" link | N/A | Recovery popup opens with fields: Name, How to Recover (radio), Recovery Email, and buttons Send / Close. | Untested |
| **FLI_002** | Functional | P0 | Successful recovery with valid Name and Recovery Email | 1. Open the popup<br>2. Enter a valid Name<br>3. Select "Reset passwords by Recovery Email"<br>4. Enter the Recovery Email registered to the account<br>5. Click "Send" | Name: `[valid_name]`<br>Email: `[registered_email]` | System sends a Login Information Email to the registered address. A success message is displayed. | Untested |
| **FLI_003** | Functional | P0 | Recovery with Name that does not match the Recovery Email | 1. Open the popup<br>2. Enter a Name that does not match the account's registered name<br>3. Enter a valid Recovery Email<br>4. Click "Send" | Name: `[wrong_name]`<br>Email: `[registered_email]` | System displays an error indicating the Name and Recovery Email do not match any account. No email is sent. | Untested |
| **FLI_004** | Functional | P0 | Recovery with correct Name but unregistered Recovery Email | 1. Open the popup<br>2. Enter the correct Name<br>3. Enter an email not registered to any account<br>4. Click "Send" | Name: `[valid_name]`<br>Email: `[unregistered_email]` | System displays an error indicating the email address is not found. No email is sent. | Untested |
| **FLI_005** | Validation | P1 | Submit with all fields empty | 1. Open the popup<br>2. Leave Name and Recovery Email empty<br>3. Click "Send" | N/A | System prompts user to fill in the required fields (Name and Recovery Email). Request is not submitted. | Untested |
| **FLI_006** | Validation | P1 | Submit with Name empty | 1. Open the popup<br>2. Leave Name empty<br>3. Enter a valid Recovery Email<br>4. Click "Send" | Name: _(empty)_<br>Email: `[valid_email]` | System prompts user to enter the Name field. Request is not submitted. | Untested |
| **FLI_007** | Validation | P1 | Submit with Recovery Email empty | 1. Open the popup<br>2. Enter a valid Name<br>3. Leave Recovery Email empty<br>4. Click "Send" | Name: `[valid_name]`<br>Email: _(empty)_ | System prompts user to enter the Recovery Email field. Request is not submitted. | Untested |
| **FLI_008** | Validation | P1 | Submit with invalid email format | 1. Open the popup<br>2. Enter a valid Name<br>3. Enter a malformed email address<br>4. Click "Send" | Name: `[valid_name]`<br>Email: `notanemail`, `user@`, `@domain.com` | System displays a validation error indicating the email format is invalid. Request is not submitted. | Untested |
| **FLI_009** | Functional | P1 | "Reset passwords by Recovery Email" radio button is selected by default | 1. Open the popup<br>2. Observe the "How to Recover" radio button state | N/A | "Reset passwords by Recovery Email" radio button is selected by default without user interaction. | Untested |
| **FLI_010** | Functional | P1 | Close popup via "Close" button | 1. Open the popup<br>2. Click the "Close" button | N/A | Popup closes and user returns to the login page. No action is taken. | Untested |
| **FLI_011** | Functional | P2 | Recovery email is sent to the correct inbox | 1. Submit a valid recovery request<br>2. Check the inbox of the registered Recovery Email | Name: `[valid_name]`<br>Email: `[registered_email]` | A Login Information Email arrives in the inbox with the correct account details and reset link/instructions. | Untested |
| **FLI_012** | Functional | P2 | History of changed password sent to Data Proprietor and Master ID | 1. Submit a valid recovery request<br>2. Check the inbox of the company's Data Proprietor and Master ID | Valid recovery request | An email notifying of the password change history is sent to both the Data Proprietor and the Master ID. | Untested |
| **FLI_013** | Functional | P2 | Recovery email lands in spam when not in inbox | 1. Submit a valid recovery request<br>2. Check the spam/junk folder if email is not found in inbox | Valid recovery request | Recovery email is locatable in the spam/junk folder as noted in the UI instructions. | Untested |
| **FLI_014** | Functional | P2 | "Master ID User without Recovery Email" link is clickable | 1. Open the popup<br>2. Click the blue link "If you are a Master ID User but do not remember your Recovery Email or Address (click)" | N/A | User is redirected to or shown the alternative recovery flow for Master ID Users. | Untested |
| **FLI_015** | UI/UX | P2 | Popup renders all fields and information text correctly | 1. Open the popup<br>2. Inspect all visible elements | N/A | Popup displays: Title, Name field, How to Recover radio, Recovery Email field, instruction text (4 bullet points + blue link), Send button, Close button — all visible without overlap or truncation. | Untested |
| **FLI_016** | UI/UX | P2 | Instruction text is fully readable | 1. Open the popup<br>2. Read the instruction block | N/A | All four instruction lines and the blue Master ID link are fully displayed and legible. | Untested |
| **FLI_017** | UI/UX | P2 | Tab key navigation within popup | 1. Open the popup<br>2. Press Tab to navigate through all interactive elements | N/A | Focus moves in logical order: Name → Recovery Email → Send button → Close button. | Untested |
| **FLI_018** | UI/UX | P2 | Submit popup using Enter key | 1. Open the popup<br>2. Fill in valid Name and Recovery Email<br>3. Press Enter | Name: `[valid_name]`<br>Email: `[registered_email]` | Form is submitted, same behavior as clicking "Send". | Untested |
| **FLI_019** | Edge Case | P2 | Name field with only whitespace | 1. Open the popup<br>2. Enter only spaces in the Name field<br>3. Enter a valid Recovery Email<br>4. Click "Send" | Name: `"   "`<br>Email: `[registered_email]` | System treats whitespace-only Name as empty and prompts user to enter a valid Name. | Untested |
| **FLI_020** | Edge Case | P2 | Recovery Email field with only whitespace | 1. Open the popup<br>2. Enter a valid Name<br>3. Enter only spaces in the Recovery Email field<br>4. Click "Send" | Name: `[valid_name]`<br>Email: `"   "` | System treats whitespace-only email as empty and prompts user to enter a valid email. | Untested |
| **FLI_021** | Edge Case | P2 | Name with special characters | 1. Open the popup<br>2. Enter a Name containing special characters<br>3. Enter a valid Recovery Email<br>4. Click "Send" | Name: `O'Brien`, `José`, `<script>alert(1)</script>` | System handles special characters safely. If the name matches the registered name, email is sent. No XSS or crash occurs. | Untested |
| **FLI_022** | Edge Case | P2 | Recovery Email with leading/trailing spaces | 1. Open the popup<br>2. Enter valid Name<br>3. Enter a Recovery Email with leading/trailing spaces (e.g., " user@example.com ")<br>4. Click "Send" | Email: `" [registered_email] "` | System either trims the spaces and processes the email, or shows a format validation error. No crash occurs. | Untested |
| **FLI_023** | Edge Case | P3 | Extremely long Name input | 1. Open the popup<br>2. Enter a 500+ character string in the Name field<br>3. Click "Send" | Name: 500+ character string | System does not crash; input is either truncated at field level or an appropriate validation error is displayed. | Untested |
| **FLI_024** | Edge Case | P3 | Extremely long Recovery Email input | 1. Open the popup<br>2. Enter a 500+ character string in the Recovery Email field<br>3. Click "Send" | Email: 500+ character string | System does not crash; input is either truncated or a validation error is shown. | Untested |
| **FLI_025** | Security | P1 | SQL injection attempt in Name field | 1. Open the popup<br>2. Enter SQL injection payload in the Name field<br>3. Enter any email<br>4. Click "Send" | Name: `' OR '1'='1`<br>Email: `test@test.com` | System handles input safely. No database error is exposed and no unauthorized data is returned. | Untested |
| **FLI_026** | Security | P1 | XSS attempt in Recovery Email field | 1. Open the popup<br>2. Enter XSS payload in the Recovery Email field<br>3. Click "Send" | Email: `<script>alert('xss')</script>@test.com` | System rejects or sanitizes the input. No script executes in the browser. | Untested |
| **FLI_027** | Security | P2 | Rate limiting on Send button | 1. Open the popup<br>2. Submit valid recovery request multiple times rapidly | Valid Name and Email | System limits repeated recovery requests (e.g., rate-limiting, CAPTCHA, or cooldown) to prevent email flooding. | Untested |
| **FLI_028** | UI/UX | P3 | Responsive layout — popup on mobile screen width | 1. Open popup on a mobile device or at 375px viewport width | N/A | Popup layout adjusts properly; all fields, instruction text, and buttons are fully usable and not cropped. | Untested |
| **FLI_029** | UI/UX | P3 | Cross-browser — popup in Chrome | 1. Open popup in Google Chrome (latest)<br>2. Submit a valid recovery request | Valid credentials | Popup renders correctly and sends recovery email in Chrome. | Untested |
| **FLI_030** | UI/UX | P3 | Cross-browser — popup in Safari | 1. Open popup in Safari (latest)<br>2. Submit a valid recovery request | Valid credentials | Popup renders correctly and sends recovery email in Safari. | Untested |
